export const dynamic = "force-dynamic"

import Logout from "@/components/auth/Logout"

const page = () => {
    return (
        <div>
            <Logout />
        </div>
    )
}

export default page
