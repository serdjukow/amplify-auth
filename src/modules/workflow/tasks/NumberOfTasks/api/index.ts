// import { getOfferListByStatus } from "modules/offers/api";

export const getNumberOfTasks = async () => {
  // const [contractsToProcess] = await Promise.all([
  //   getOfferListByStatus("ContractSigned"),
  // ]);

  // const numberOfTasks = contractsToProcess.length;

  return {
    numberOfTasks: 0,
    // contractsToProcess: contractsToProcess.length,
    contractsToProcess: 0,
  };
};
